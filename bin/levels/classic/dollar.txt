if (playing&&this.init==0) { 
  timeout=0.1;
  this.init=1; 
}
if (gameover) {
  itemcount--; 
  destroy; 
}

if (timeout) {
  if (abs((x+0.5)-mainx)<=1&&abs((y+0.5)-mainy)<=1) {
    score+=5;
    darts=random(0, 3);
    if(darts<1) addenemy=1;
    else if(darts<2) addenemy=2;
    else addenemy=0;
    itemcount--;
    play extra.wav;
    destroy;
  }
  timeout=0.1;
}
