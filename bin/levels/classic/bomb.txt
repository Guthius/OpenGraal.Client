if (playing && this.init == 0) { timeout = .1; this.init = 1; dontblock; }
if (killedplayer) { itemcount--; destroy; }

if (timeout) {
  if (x - 1 <= mainx && mainx < x + 1 && y - 1 <= mainy && mainy < y + 1) {
    score += 10; booms += 10;
	darts = random(0, 3);
	if (darts < 1) addenemy = 1;
	else if (darts < 2) addenemy = 2;
	else addenemy = 0;
	itemcount--;
	destroy;
  }
  timeout = .1;
}
