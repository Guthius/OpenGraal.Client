if (playerenters) {
  timeout = 0.05;
  play wind.wav;
 
}

if (playertouchsme) {
  dontblock;
  drawoverplayer;
}

if (timeout) {
  timeout = 0.05;
  y-=0.7;
  if (y<25) {
    putleaps 4,x,y;
    set hitssm;
    destroy;
  }
}